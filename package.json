{
  "name": "karibu-health",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run dev --workspace=apps/mobile",
    "web": "npm run dev --workspace=apps/web",
    "db:migrate": "npm run migrate --workspace=packages/supabase",
    "db:types": "npm run generate-types --workspace=packages/supabase"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}

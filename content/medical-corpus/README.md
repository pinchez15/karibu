# Medical Corpus

This folder is the future canonical home for source corpus metadata and approved medical references used by Karibu EHR AI assistance and Karibu Learn case generation.

The existing root `Medical Corpus/` folder still contains current PDFs. Move those files only after updating scripts and references that read from the old path.

